const fetch = require("node-fetch");

/*async function getGroupId() {
    // if we ask for their access token and want to set everything up for them
    // then, store GROUP_ID as well as BOT_ID
    fetch("https://api.groupme.com/v3/groups?token="+process.env.BOT_ID)
        .then(res => res.json())
        .then(json => console.log(json))
        .catch(e => console.error);
    fetch("https://api.groupme.com/v3/bots?token="+)
        .then(res => res.json())
        .then(json => console.log(json))
        .catch(e => console.error);
}*/

function setName(name) {
}

//module.exports = { getGroupId };